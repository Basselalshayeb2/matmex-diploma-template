% !TeX spellcheck = ru_RU
% !TEX root = stommis.tex

\clearpage

\section{Постановка задачи}
\label{sec:task}

Целью работы является разработка прототипа подсистемы голосового управления для МИС «СТОММИС», позволяющей врачу работать с медицинской картой \emph{в процессе лечения} с помощью голоса, без необходимости использовать клавиатуру/мышь и без сценария обязательного нажатия кнопки \emph{push-to-talk}.
При этом решение должно быть ресурсоэффективным: не выполнять постоянное распознавание всего аудиопотока и минимизировать сетевой трафик.

Для достижения цели были поставлены следующие задачи.
\begin{enumerate}
    \item Провести обзор существующих решений в области голосового управления и выделения команд в медицинских информационных системах.
    \item Спроектировать архитектуру подсистемы голосового управления для МИС «СТОММИС», определить компоненты: браузерный клиент, сервер обработки, сервис распознавания речи, модуль интерпретации, интерфейс интеграции с МИС, границы ответственности и основной сценарий взаимодействия.
    \item Разработать клиентский модуль захвата аудио-потока в браузере с выделением участков с речью/без речи (VAD) в реальном времени (раздел~\ref{subsec:task1}).
    \item Интегрировать обработку аудиосегментов с серверным контуром: приём аудиофайла, транскодирование при необходимости, распознавание речи (ASR) с использованием сервиса распознавания речи Yandex SpeechKit и интерпретация распознанного текста в структурированное действие для заполнения полей карты пациента в МИС «СТОММИС» (раздел~\ref{subsec:task3}).
    \item Реализовать механизм выделения команд без необходимости постоянного произнесения слова активации: после распознавания речи (ASR) применять на сервере бинарный классификатор \textit{command/not-command}, определяющий, требуется ли запуск дальнейшей обработки (LLM и выполнение действия в МИС «СТОММИС») (раздел~\ref{subsec:text_classifier}).
    \item Провести экспериментальную валидацию прототипа, включая апробацию в реальных условиях (СПб ГБУЗ «СП №8»): оценить задержку, потребление ресурсов, частоту ложных срабатываний/пропусков и потенциальное снижение затрат по сравнению с вариантом постоянного распознавания аудиопотока (раздел~\ref{sec:experiment}).
\end{enumerate}


% Todo: we can move thi ssection to (обзор или введение)
% В качестве контрольных вариантов в ходе работы также рассматривались ручная активация и активация по ключевому слову.

% Ограничения и допущения работы:
% \begin{itemize}
%     \item Решение должно исполняться в стандартном браузере (JavaScript) и/или на сервере в поликлинике (Python) без специализированного оборудования.
%     \item Система должна быть устойчивой к типовым акустическим помехам стоматологического кабинета (разговоры, шум оборудования).
%     \item Допустимая задержка от окончания произнесения команды до выполнения действия в МИС «СТОММИС» должна оставаться приемлемой для врача (целевое значение порядка нескольких секунд).
%     \item Должны быть предусмотрены механизмы устойчивости к ошибкам ASR (неполные/искажённые транскрипции) и к ошибкам LLM (ошибочный intent/слоты), включая безопасное поведение по умолчанию (fail-safe).
% \end{itemize}
